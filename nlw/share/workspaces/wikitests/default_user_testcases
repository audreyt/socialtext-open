These testcases work for a Default user but not for an LDAP user, but it is possible that an LDAP user has called Default User Testcases.
Carefully set up the environment so the tests will run.

| *Comment* | Default User Testcases |  |
| *Comment* | Set up required Default user values to override possibly being logged in as an LDAP user |  |
| *Comment* | This test explicitly uses wikitester@ken.socialtext.net as the Default user |  |

| set | username | wikitester@ken.socialtext.net |
| set | password | d3vnu11l |
| set | email | wikitester@ken.socialtext.net |

| *Comment* | Default User Testcases: REMOVED call to Disable LDAP because each included test does that anyway |  |

| *Comment* | Default User Testcases: You must immediately logout since your LDAP user is gone |  |
| st-logout |  |  |

| *Comment* | Default User Testcases: You may now login as default user wikitester |  |
| st-login | %%username%% | %%password%% |

| *Comment* | Default User Testcases: By INCLUDING these tests, they will run with the overridden username, pwd, and email |  |
| include | [Test case: Login Logout] |  |
| include | [Test case: User Resets Password] |  |
| include | [Test case: Admin Resets Password] |  |

| *Comment* | Default User Testcases: Re-enable LDAP. Bring the LDAP user back to life |  |
| include | [Enable LDAP] |  |

| *Comment* | Default User Testcases COMPLETED |  |

Test Case: User Restrictions moved to [Stubborn Testcases]
