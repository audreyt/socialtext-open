#!/usr/bin/env perl
# @COPYRIGHT@

use strict;
use warnings;
use FindBin;
use lib "$FindBin::Bin/../lib";
use Socialtext::SystemSettings qw(get_system_setting);

print get_system_setting('default-account')->name;
exit 0;
